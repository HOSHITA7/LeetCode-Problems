class Solution {

    public int countSeniors(String[] details) {
        
        int c = 0;

        for (var v : details) {

            int a = Integer.parseInt(v.substring(11, 13));

            if (a > 60)  c++;
        }

        return c;
    }
}
